# workflow


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Session State Store Adapter

Bridges the pure `SQLiteWorkflowStateStore` (which accepts
`session_id: str`) to the `WorkflowStateStore` protocol expected by
`StepFlow` (which passes `sess` objects).

## StructureDecompWorkflow

Main workflow class that orchestrates the structure decomposition
process. It creates and manages:

- **SQLiteWorkflowStateStore**: Persistent state storage for workflow
  progress
- **Services**: Source, Segmentation, Alignment, and Graph services
- **StepFlow**: 3-step wizard with navigation
- **Routes**: API endpoints for the workflow

------------------------------------------------------------------------

### StructureDecompWorkflow

``` python

def StructureDecompWorkflow(
    plugin_manager:PluginManager, # Plugin manager from host application
    config:Optional=None, # Workflow configuration
    on_complete_redirect_url:Optional=None, # If set, "Start New Workflow" redirects here instead of resetting in place
):

```

*Self-contained structure decomposition workflow.*

------------------------------------------------------------------------

### StructureDecompWorkflow.setup

``` python

def setup(
    app, # FastHTML application instance
)->None:

```

*Initialize workflow with FastHTML app.*

------------------------------------------------------------------------

### StructureDecompWorkflow.cleanup

``` python

def cleanup(
    
)->None:

```

*Clean up workflow resources.*

------------------------------------------------------------------------

### StructureDecompWorkflow.get_routers

``` python

def get_routers(
    
)->List: # List of routers to register

```

*Return all routers for registration with the app.*

------------------------------------------------------------------------

### StructureDecompWorkflow.render_entry_point

``` python

def render_entry_point(
    request, # FastHTML request object
    sess, # FastHTML session object
):

```

*Render the workflow entry point for embedding.*

## Shared StepFlow Utilities

## Selection Step

## Segmentation Step

## Review Step

## Verify Step
